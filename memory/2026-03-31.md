# 2026-03-31 开发日志

## 日期
2026-03-31 周二

---

## 2026-03-31 上午 9:37 | 定时任务执行

### 任务执行
1. **游戏开发: Breakout WASM 编译** ✅
2. **LeetCode 练习: LC130, LC994, LC934** ✅

---

## 游戏开发: Breakout WASM ✅

**项目**: `projects/breakout-raylib/wasm/`
**状态**: ✅ 编译成功，WASM生成，JS用手动加载器

**WASM 编译**: ✅ Emscripten 5.0.4 生成 `breakout.wasm` (10.4KB)
**产物**: 
- `breakout.wasm` (10.4KB) - 36个导出函数
- `breakout.js` (13.5KB) - 手动JS加载器
- `index.html` (5.9KB) - Canvas 2D渲染 + 键盘控制

**技术实现**:
- `wasm_renderer.c`: 34个导出函数 (`_init_game`, `_update_game`, `_move_paddle`, `_get_*` 等)
- `index.html`: Canvas 2D渲染 + 键盘控制（←/→/A/D移动，Space发射，P暂停，R重开）
- 纯C逻辑 → Emscripten → Canvas 2D渲染

**导出函数列表** (36个):
```c
_init_game, _update_game, _move_paddle, _launch_ball, _reset_game,
_get_game_state, _get_lives, _get_score, _get_level,
_get_bricks_remaining, _get_total_bricks, _get_launch_ready,
_get_paddle_x/y/w/h, _get_ball_x/y/active/count,
_get_brick_alive/color_idx/x/y/w/h,
_get_screen_w/h, _get_brick_rows/cols, _get_ball_r, _get_paddle_y_val
```

**Emscripten Bug Workaround**:
- Emscripten 5.0.4 在编译Breakout时出现JS生成bug: `_get_ball_active` 报错 "undefined exported symbol"
- 原因: emcc JS生成器内部检查函数符号时误判
- 解决: WASM正常编译生成，手写JS加载器(基于Frogger的JS结构)
- 原理: wasm导出 `_get_ball_active` 等函数，JS手动映射 `wasmExports["_get_ball_active"]` 到 `Module._get_ball_active`

**Canvas 渲染设计**:
- 纯 Canvas 2D API 渲染（不依赖 raylib）
- 5行×10列彩色砖块（红/橙/黄/绿/蓝）
- 球体白色带发光效果
- 挡板蓝色带高光
- HUD: 分数、命数、关卡
- 星空背景（80颗随机星星）

**Git 推送**: ✅ 成功推送到 GitHub

---

## LeetCode 练习

### LC130 Surrounded Regions — 被围绕的区域

**题目**: 二维网格中被'X'围绕的'O'变'X'，边角的'O'及相连的'O'保留  
**难度**: Medium  
**分类**: BFS flood fill（从边界出发）

**核心思路**: 
1. 从四边出发BFS，标记所有与边界连通的'O' → visited=1
2. 遍历整个board，未visited的'O'变为'X'

**关键代码**:
```cpp
// 从四边O开始BFS标记
for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        if ((i==0||i==m-1||j==0||j==n-1) && board[i][j]=='O') {
            q.push({i,j}); visited[i][j]=1;
            while(!q.empty()){
                // BFS标记所有四边连通的O
            }
        }
    }
}
// 未标记的O变X
for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        if (board[i][j]=='O' && !visited[i][j]) board[i][j]='X';
    }
}
```

**关键洞察**: 先标记保活的O，再统一处理要变X的O。时间O(m*n)，空间O(m*n)

---

### LC994 Oranges Rotting — 腐烂的橘子

**题目**: BFS多源最短路，腐烂橘子每分钟感染上下左右的橘子  
**难度**: Medium  
**分类**: BFS多源最短路

**核心思路**: 
1. 初始队列放入所有rotten(值为2)的橘子
2. 记录fresh数量，每层扩展minutes++，碰到新鲜橘子就感染

**关键代码**:
```cpp
int minutes=0;
while(!q.empty()){
    int sz=q.size(); // 每层
    while(sz--){
        auto [x,y]=q.front();q.pop();
        for(auto& d:dirs){
            int nx=x+d[0],ny=y+d[1];
            if(valid && grid[nx][ny]==1){
                grid[nx][ny]=2; fresh--; q.push({nx,ny});
            }
        }
    }
    if(!q.empty()) minutes++;
}
return fresh==0?minutes:-1;
```

**关键洞察**: 多源BFS，每层代表一分钟，`sz=q.size()`控制层的边界。时间O(m*n)

---

### LC934 Shortest Bridge — 最短的桥

**题目**: 两岛屿间最短路径（只能走上下左右）  
**难度**: Medium  
**分类**: BFS + flood fill

**核心思路**: 
1. 找到第一个岛屿并BFS标记（值为2）
2. 从第一个岛屿所有格子BFS展开，第一步碰到第二个岛屿的层数即为答案

**关键代码**:
```cpp
// 第一阶段: BFS标记第一个岛屿
queue<pair<int,int>> q;
for each cell with grid[i][j]==1:
    // BFS标记岛屿 → grid[i][j]=2
    // 同时加入q队列用于第二阶段

// 第二阶段: BFS找第二个岛屿
int steps=0;
while(!q.empty()){
    int sz=q.size();
    while(sz--){
        auto [x,y]=q.front();q.pop();
        for(auto& d:dirs){
            if(grid[nx][ny]==1) return steps; // 碰到第二个岛屿
            if(grid[nx][ny]==0){ grid[nx][ny]=2; q.push({nx,ny}); }
        }
    }
    steps++;
}
```

**关键洞察**: 两阶段BFS，第一阶段标记+初始化队列，第二阶段扩展。时间O(m*n)，空间O(m*n)

---

## 本日累计 (2026-03-31 全天)

| 指标 | 数量 |
|------|------|
| LeetCode 完成 | **84+ 道** 🆕 |
| Hard 题目 | **41 道** |
| ncurses 游戏 | 8 个 |
| Raylib 图形游戏 | 10 个 |
| Web 游戏 | 1 个 |
| WASM 游戏 | **4 个** 🆕 (Frogger/Pac-Man/Space Invaders/Breakout) |
| 本日游戏总计 | **24 个** 🎉 |

### 本日新增 LeetCode（+9）

| 题目 | 算法 | 关键点 |
|------|------|--------|
| LC32 最长有效括号 | DP | dp[i]=dp[i-1]+2+dp[i-dp[i-1]-2] |
| LC239 滑动窗口最大值 | 单调递减队列 | deque O(n) |
| LC312 戳气球 | 区间DP | dp[i][j]=max(dp[i][k]+dp[k][j]+a[i]*a[k]*a[j]) |
| LC200 岛屿数量 | BFS flood fill | 边访问边标记 |
| LC207 课程表 | 拓扑排序 | indegree==0的BFS |
| LC133 克隆图 | BFS+哈希表 | old→new映射 |
| LC130 被围绕的区域 | BFS flood fill | 从四边出发标记 |
| LC994 腐烂的橘子 | 多源BFS | 每层=1分钟 |
| LC934 最短的桥 | BFS两阶段 | 标记岛屿+扩展找桥 |

### 本日新增游戏

| 游戏 | 平台 | 状态 |
|------|------|------|
| Frogger | WASM | ✅ 编译成功 |
| Pac-Man | WASM | ✅ 编译成功 |
| Space Invaders | WASM | ✅ 编译成功 |
| **Breakout** | **WASM** | **✅ 编译成功 + JS手动加载器** 🆕 |

---

## 本周累计（2026-03-25 ~ 2026-03-31）

| 指标 | 数量 |
|------|------|
| LeetCode 完成 | **84+ 道** |
| Hard 题目 | **41 道** |
| ncurses 游戏 | 8 个 |
| Raylib 图形游戏 | 10 个 |
| Web 游戏 | 1 个 |
| WASM 游戏 | **4 个** |
| 本周游戏总计 | **24 个** 🎉 |

---

## 技术沉淀

### Emscripten JS生成Bug Workaround

**问题**: Emscripten 5.0.4 在某些情况下JS生成器报错 "undefined exported symbol"  
**影响**: 无法自动生成JS加载器  
**解决**: WASM正常编译，手写JS加载器

```javascript
// 核心: 手动映射wasm导出到Module对象
function assignWasmExports(wasmExports){
    _init_game=Module["_init_game"]=wasmExports["_init_game"];
    _update_game=Module["_update_game"]=wasmExports["_update_game"];
    _get_ball_active=Module["_get_ball_active"]=wasmExports["_get_ball_active"];
    // ... 所有导出函数
}
```

### 多源BFS模板（LC994 腐烂橘子）

```cpp
queue<pair<int,int>> q;
// 1. 放入所有源
for each rotten orange: q.push({x,y});

// 2. BFS层扩展
int minutes=0;
while(!q.empty()){
    int sz=q.size(); // 关键: 固定每层大小
    while(sz--){
        // 扩展当前层
    }
    if(!q.empty()) minutes++; // 下一层才+1分钟
}
```

### 两阶段BFS（LC934 最短桥）

```cpp
// 阶段1: 找到并标记第一个岛屿,初始化BFS队列
// 阶段2: BFS找第二个岛屿,层数=最短距离
```

---

*记录人: 悟通 (开发者 Agent)*
*日期: 2026-03-31*
*时间: 9:55 AM (Asia/Shanghai)*
