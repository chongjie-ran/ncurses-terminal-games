# 2026-03-31 开发日志

## 日期
2026-03-31 周二

---

## 2026-03-31 凌晨3:37 | 定时任务执行

### LeetCode 练习

| 题目 | 分类 | 难度 | 结果 |
|------|------|------|------|
| LC32 最长有效括号 | DP | Hard | ✅ |
| LC239 滑动窗口最大值 | 单调队列 | Hard | ✅ |

### LeetCode 关键题目（今日新增）

| 题目 | 算法 |
|------|------|
| LC32 Longest Valid Parentheses | DP（栈辅助） |
| LC239 Sliding Window Maximum | 单调递减队列 O(n) |

---

## 游戏开发：Frogger WASM ✅

**项目**: `projects/frogger-raylib/wasm/`
**Commit**: `frogger-wasm-v1` (in progress)
**WASM 编译**: ✅ **零错误**通过（Emscripten 5.0.4）
**产物**: `frogger.js` (12KB) + `frogger.wasm` (3.7KB)

**技术实现**:
- `game.h/game.c` 纯C逻辑 → Emscripten 编译到 WASM
- `wasm_renderer.c`: WASM 导出 C 函数（无 raylib 依赖）
- `index.html`: Canvas 2D 渲染 + 键盘控制（Arrow/WASD）
- 使用 `EMSCRIPTEN_KEEPALIVE` 导出 C 函数给 JS 调用
- `Module.cwrap()` / `Module.ccall()` 从 JS 调用 WASM 函数

**WASM 架构**:
```
game.c (纯C逻辑)
    ↓ Emscripten 编译
wasm_renderer.c (导出函数接口)
    ↓
frogger.wasm + frogger.js
    ↓
index.html (Canvas 2D 渲染 + 键盘输入)
```

**导出函数列表**:
```c
_init_game, _update_game, _get_game_state,
_get_frog_x, _get_frog_y, _get_frog_alive, _get_frog_lives,
_get_time_left, _get_homes_filled, _get_score,
_get_obs_count, _get_obs_x, _get_obs_y, _get_obs_active,
_get_log_count, _get_log_x, _get_log_y, _get_log_active,
_get_safe_zone, _get_invincible_timer,
_move_frog, _reset_frog,
_get_grid_h, _get_grid_w, _get_cell
```

**Canvas 渲染设计**:
- 纯 Canvas 2D API 渲染（不依赖 raylib）
- 道路（灰）、草地（绿）、水道（蓝）分区域绘制
- 青蛙带眼睛闪动效果（受伤时闪烁）
- 车辆多彩圆角矩形，木头纹理
- 荷叶/终点区域金黄色

---

## 2026-03-31 凌晨5:40 | 定时任务执行

### LeetCode 练习

| 题目 | 算法 | 结果 |
|------|------|------|
| LC312 戳气球 | 区间DP | ✅ |

### LeetCode 关键题目（新增）

| 题目 | 算法 | 关键洞察 |
|------|------|---------|
| LC312 Burst Balloons | 区间DP | 枚举最后戳破的k，添加虚拟气球=1 |

### 游戏开发：Pac-Man WASM ✅

**项目**: `projects/pacman-raylib/wasm/`
**WASM 编译**: ✅ **零错误**通过（Emscripten 5.0.4）
**产物**: `pacman.js` (12.7KB) + `pacman.wasm` (5.9KB)

**技术实现**:
- `game.h/game.c` 纯C逻辑 → Emscripten 编译到 WASM
- `wasm_renderer.c`: 23个导出函数，无 raylib 依赖
- `index.html`: Canvas 2D 渲染 + 键盘控制（Arrow/WASD/P/R）

**导出函数列表**:
```c
_init_game, _update_game, _get_game_state,
_get_score, _get_lives, _get_dots_eaten, _get_level,
_get_pacman_x, _get_pacman_y, _get_pacman_dir,
_get_ghost_count, _get_ghost_x, _get_ghost_y,
_get_ghost_type, _get_ghost_frightened, _get_ghost_eaten, _get_ghost_dir,
_get_board_cell, _set_dir, _reset_game,
_get_cols, _get_rows, _get_cell, _get_frightened_timer
```

**Canvas 渲染设计**:
- 纯 Canvas 2D API 渲染（不依赖 raylib）
- 墙壁蓝色方块，豆子粉色小圆，能量豆大圆
- Pac-Man 带张嘴动画（方向决定开口角度）
- 幽灵彩色身体+眼睛，frightened模式变蓝+闪烁
- 剩余生命显示在画布顶部

**Emscripten 编译教训**:
- 必须 `#include <emscripten/emscripten.h>` 获取 `EMSCRIPTEN_KEEPALIVE` 宏
- 否则报错：`error: variable has incomplete type 'void'`

---

## 2026-03-31 早上7:40 | 定时任务执行

### LeetCode 练习

| 题目 | 算法 | 结果 |
|------|------|------|
| LC200 岛屿数量 | BFS/DFS flood fill | ✅ |
| LC207 课程表 | 拓扑排序 BFS/DFS | ✅ |
| LC133 克隆图 | BFS/DFS + 哈希表 | ✅ |

### LeetCode 关键题目（今日新增）

| 题目 | 算法 | 关键洞察 |
|------|------|----------|
| LC200 Number of Islands | BFS flood fill | 遇到'1'就BFS展开，同时标记为'0' |
| LC207 Course Schedule | 拓扑排序 Kahn算法 | `indegree==0`的节点先处理，有环则visited<numCourses |
| LC133 Clone Graph | BFS + unordered_map | `old→new`映射，边遍历边构建新图 |

### 游戏开发：Space Invaders WASM ✅

**项目**: `projects/space-invaders-raylib/wasm/`
**WASM 编译**: ✅ **零错误**通过（Emscripten 5.0.4）
**产物**: `space_invaders.js` (13.7KB) + `space_invaders.wasm` (19KB)

**技术实现**:
- `game.h/game.c` 纯C逻辑 → Emscripten 编译到 WASM
- `wasm_renderer.c`: 34个导出函数，无 raylib 依赖
- `index.html`: Canvas 2D 渲染 + 键盘控制（Arrow/WASD/Space/P/R）

**导出函数列表**:
```c
_init_game, _update_game, _get_game_state,
_get_score, _get_lives, _get_wave, _get_alive_count,
_get_player_x, _get_player_y,
_get_bullet_count, _get_bullet_x/y/active,
_get_enemy_bullet_count, _get_enemy_bullet_x/y/active,
_get_alien_count, _get_alien_x/y/alive/type/exploding,
_move_left, _move_right, _shoot, _reset_game,
_get_screen_w/h, _get_player_w/h, _get_alien_w/h/rows/cols
```

**Canvas 渲染设计**:
- 纯 Canvas 2D API 渲染（不依赖 raylib）
- 星空背景（60颗随机星星）
- 玩家飞船（绿色，带炮管）
- 外星人按行着色：红/橙/黄/绿，触角动画
- 玩家子弹绿色，敌人子弹红色
- HUD显示：分数、波次、生命、外星人数量
- 支持暂停(P)、重开(R)

**Git 教训**:
- `git reset --hard origin/main` 会丢失未跟踪的文件
- 解决方案：使用独立分支 `origin/space-invaders` 恢复源码
- 教训：重要未提交的代码应及时 commit 或推送到分支

**签名匹配教训**:
- game.c 中的函数名是 `initGame`（非 `init_game`）
- 必须匹配 game.c 的实际签名：`initGame(Game*)`, `updateGame(Game*, float)`, `fireBullet(Game*)`
- wasm_renderer.c 需声明这些函数以便链接器找到

---

## 本日累计 (2026-03-31 全天)

| 指标 | 数量 |
|------|------|
| LeetCode 完成 | **81+ 道** |
| Hard 题目 | **41 道** |
| ncurses 游戏 | 8 个 |
| Raylib 图形游戏 | 10 个 |
| Web 游戏 | 1 个 |
| WASM 游戏 | **3 个** |
| 本日游戏总计 | **23 个** 🎉 |

### 本日新增 LeetCode（+6）

| 题目 | 算法 | 关键点 |
|------|------|--------|
| LC32 最长有效括号 | DP | `dp[i]=dp[i-1]+2+dp[i-dp[i-1]-2]` 处理嵌套 |
| LC239 滑动窗口最大值 | 单调递减队列 | deque 存索引，O(n) 复杂度 |
| LC312 戳气球 | 区间DP | `dp[i][j]=max(dp[i][k]+dp[k][j]+a[i]*a[k]*a[j])` |
| LC200 岛屿数量 | BFS flood fill | 遇到'1'→BFS→全标记'0'，O(m*n) |
| LC207 课程表 | 拓扑排序 | indegree==0的BFS，有环则visited<numCourses |
| LC133 克隆图 | BFS+哈希表 | `old→new`映射，边遍历边构建 |

### 本日新增游戏

| 游戏 | 平台 | 状态 |
|------|------|------|
| Frogger | WASM | ✅ 编译成功 |
| Pac-Man | WASM | ✅ 编译成功 |
| Space Invaders | WASM | ✅ 编译成功 + GitHub推送成功 🆕 |

---

## 本周累计（2026-03-25 ~ 2026-03-31）

| 指标 | 数量 |
|------|------|
| LeetCode 完成 | **81+ 道** |
| Hard 题目 | **41 道** |
| ncurses 游戏 | 8 个 |
| Raylib 图形游戏 | 10 个 |
| Web 游戏 | 1 个 |
| WASM 游戏 | **3 个** |
| 本周游戏总计 | **23 个** 🎉 |

---

## 技术沉淀

### LC312 区间DP模板

```cpp
int maxCoins(vector<int>& nums) {
    int n = nums.size();
    vector<int> a(n + 2, 1);          // 添加虚拟气球
    for (int i = 0; i < n; i++) a[i + 1] = nums[i];
    n = a.size();

    vector<vector<int>> dp(n, vector<int>(n, 0));
    for (int len = 3; len <= n; len++) {
        for (int i = 0; i + len <= n; i++) {
            int j = i + len - 1;
            for (int k = i + 1; k < j; k++) {
                dp[i][j] = max(dp[i][j],
                    dp[i][k] + dp[k][j] + a[i] * a[k] * a[j]);
            }
        }
    }
    return dp[0][n - 1];
}
```

### 单调队列模板

```cpp
deque<int> dq; // 存索引，值单调递减
for (int i = 0; i < n; i++) {
    while (!dq.empty() && nums[dq.back()] <= nums[i])
        dq.pop_back();
    dq.push_back(i);
    if (dq.front() <= i - k) dq.pop_front();
    if (i >= k - 1) ans.push_back(nums[dq.front()]);
}
```

### Emscripten WASM 编译步骤

```bash
source ~/emsdk/emsdk_env.sh

emcc \
  game.c wasm_renderer.c \
  -o game.js \
  -s WASM=1 \
  -s EXPORTED_FUNCTIONS='["_init_game","_update_game",...]' \
  -s EXPORTED_RUNTIME_METHODS='["ccall","cwrap"]' \
  -s ALLOW_MEMORY_GROWTH=1 \
  -s MODULARIZE=1 \
  -s EXPORT_NAME="Module" \
  --no-entry -O2
```

### Emscripten 必须包含头文件

```c
#include <emscripten/emscripten.h>  // 必须！否则 EMSCRIPTEN_KEEPALIVE 宏未定义
// 报错: variable has incomplete type 'void'
```

### WASM + Canvas 渲染架构

```javascript
// 1. 加载 WASM 模块
var Module = {
  onRuntimeInitialized: function() { initGame(); }
};

// 2. 调用 C 函数
Module.ccall('init_game', null, [], []);
Module._update_game(dt);

// 3. 读取游戏状态
const frogX = Module._get_frog_x();
const lives = Module._get_frog_lives();

// 4. Canvas 2D 渲染
ctx.fillStyle = '#4ade80';
ctx.beginPath();
ctx.arc(frogX + CELL/2, frogY + CELL/2, CELL*0.4, 0, Math.PI*2);
ctx.fill();
```

### 图问题 BFS/DFS 模板

```cpp
// LC200 Number of Islands - BFS flood fill
int numIslands(vector<vector<char>>& grid) {
    int m=grid.size(), n=grid[0].size(), count=0;
    vector<vector<int>> dirs = {{1,0},{-1,0},{0,1},{0,-1}};
    for (int i=0;i<m;i++) for (int j=0;j<n;j++) {
        if (grid[i][j]=='1') {
            count++;
            queue<pair<int,int>> q; q.push({i,j}); grid[i][j]='0';
            while(!q.empty()) {
                auto [x,y]=q.front(); q.pop();
                for(auto& d:dirs) {
                    int nx=x+d[0], ny=y+d[1];
                    if(nx>=0&&nx<m&&ny>=0&&ny<n&&grid[nx][ny]=='1'){
                        grid[nx][ny]='0'; q.push({nx,ny});
                    }
                }
            }
        }
    }
    return count;
}

// LC207 Course Schedule - BFS拓扑排序
bool canFinish(int numCourses, vector<vector<int>>& prereq) {
    vector<vector<int>> adj(numCourses);
    vector<int> indegree(numCourses,0);
    for(auto& p:prereq){ adj[p[1]].push_back(p[0]); indegree[p[0]]++; }
    queue<int> q; for(int i=0;i<numCourses;i++) if(indegree[i]==0) q.push(i);
    int visited=0;
    while(!q.empty()){
        int u=q.front();q.pop();visited++;
        for(int v:adj[u]){ indegree[v]--; if(indegree[v]==0) q.push(v); }
    }
    return visited==numCourses;
}

// LC133 Clone Graph - BFS + unordered_map
Node* cloneGraph(Node* node) {
    if(!node) return nullptr;
    unordered_map<Node*,Node*> mp;
    queue<Node*> q; q.push(node); mp[node]=new Node(node->val);
    while(!q.empty()){
        Node* cur=q.front();q.pop();
        for(Node* n:cur->neighbors){
            if(mp.find(n)==mp.end()){ mp[n]=new Node(n->val); q.push(n); }
            mp[cur]->neighbors.push_back(mp[n]);
        }
    }
    return mp[node];
}
```

---

*记录人: 悟通 (开发者 Agent)*
*日期: 2026-03-31*
*时间: 8:20 AM (Asia/Shanghai)*
