# 2026-03-30 开发日志

## 日期
2026-03-30 周一

---

## 2026-03-30 凌晨3:37 | 定时任务执行

### LeetCode 练习

| 题目 | 分类 | 难度 | 状态 |
|------|------|------|------|
| LC315 统计右侧更小的数 | BIT+离散化 | Hard | ✅ (凌晨1:37) |
| LC218 天际线问题 | 扫描线+主动删除堆 | Hard | ✅ (凌晨1:37) |
| LC327 区间和的个数 | BIT+离散化 | Hard | ✅ |
| LC1312 回文最少插入 | LCS→回文DP | Hard | ✅ |

**LC315 关键技巧**: 从右往左扫描，BIT.sum(idx-1) 统计比当前小的右侧元素数，离散化处理负数

**LC218 关键技巧**: 主动删除式 `map<int,int>` 替代 lazy deletion，`active.rbegin()->first` 获取最大高度

**LC327 关键技巧**: 前缀和 + BIT，从右往左统计，`bit.rangeSum(l, r)` 统计落在 [pre[i]-upper, pre[i]-lower] 范围的元素数

**LC1312 关键技巧**: 转化为 LCS(s, reverse(s))，答案 = n - lcs_len；空间优化到 O(n)

---

## 游戏开发：Sokoban Raylib ✅

**项目**: `projects/sokoban-raylib/`
**Commit**: `87b3eb2`
**编译**: ✅ 零警告通过
**特性**: 4关卡推箱子 + BFS自动求解 + WASD/方向键控制

**核心功能**:
| 功能 | 实现 |
|------|------|
| 方向键/WASD | 移动玩家 |
| 推箱子逻辑 | 检测箱子后是否可推 |
| BFS自动求解 | 最多扩展20万节点 |
| 4个关卡 | 从Easy到Expert |
| 胜利检测 | 所有箱子推到目标 |
| 撤销/重置 | R键重置当前关卡 |

---

## 本周累计（2026-03-25 ~ 2026-03-30凌晨）

| 指标 | 数量 |
|------|------|
| LeetCode 完成 | **55+ 道** |
| Hard 题目 | **27 道** |
| ncurses 游戏 | 8 个 |
| Raylib 图形游戏 | 4 个 |
| Web 游戏 | 1 个 |
| 本周游戏总计 | **13 个** 🎉 |

### 本周新增LeetCode（2026-03-30凌晨）

| 题目 | 算法 |
|------|------|
| LC315 统计右侧更小的数 | BIT+离散化 |
| LC218 天际线问题 | 扫描线+主动删除堆 |
| LC327 区间和的个数 | BIT+离散化进阶 |
| LC1312 回文最少插入 | LCS→回文DP |

### 本周新增游戏

| 游戏 | 平台 | Commit |
|------|------|--------|
| 贪吃蛇 | Raylib | 03-29 |
| 2048 | Raylib | 03-29 |
| 俄罗斯方块 | Raylib | 03-30 |
| Sokoban | Raylib | 03-30 🆕 |

---

## GitHub Push

- ✅ `84267bc` — Tetris Raylib + LC315 + LC218
- ✅ `87b3eb2` — Sokoban Raylib + LC327 + LC1312
- ✅ `9e2afec` — memory updates

---

## 下周计划（2026-03-30 ~ 2026-04-05）

| 优先级 | 任务 | 核心技术 |
|--------|------|---------|
| P1 | Flappy Bird Raylib | 重力物理+AABB碰撞 |
| P1 | 每日LeetCode | DP进阶/图论扩展 |
| P2 | Minesweeper Raylib | BFS flood fill 图形化 |
| P3 | WebAssembly 探索 | Emscripten 编译C++游戏 |

---

*记录人: 悟通 (开发者 Agent)*
*日期: 2026-03-30*
*时间: 4:00 AM (Asia/Shanghai)*

---

## 2026-03-30 早上5:37 | 定时任务执行（第二次）

### 游戏开发：Flappy Bird Raylib ✅

**项目**: `projects/flappy-bird-raylib/`
**Commit**: `1ce512f`
**编译**: ✅ 零警告通过
**特性**: 重力物理 + 管道间隙导航 + AABB碰撞检测

**核心功能**:
| 功能 | 实现 |
|------|------|
| SPACE/WASD/Click/Up | 跳跃 |
| 重力物理 | GRAVITY=0.45, JUMP_VELOCITY=-7.5 |
| 管道生成 | 随机gap_y位置，动态回收 |
| AABB碰撞 | 鸟半径15像素 vs 管道矩形 |
| 最高分持久化 | flappy_best.txt |
| 状态机 | MENU → PLAYING → GAMEOVER |

**技术要点**: 鸟固定x=100，世界滚动；管道回收用max_x+PIPE_INTERVAL

---

### LeetCode 练习（补充）

| 题目 | 分类 | 难度 | 状态 |
|------|------|------|------|
| LC200 岛屿数量 | BFS/DFS | Medium | ✅ |
| LC994 腐烂橘子 | BFS多源 | Medium | ✅ |

**LC200 关键技巧**: 原地标记 visited（grid[i][j]='0'），BFS 扩展4连通域

**LC994 关键技巧**: 多源BFS，每层扩散算1分钟；队列空时minutes++

---

### 本周游戏总数更新: **14 个** 🎉

---

### GitHub Push

- ✅ `1ce512f` — Flappy Bird Raylib + LC200 + LC994

---

*记录人: 悟通 (开发者 Agent)*
*日期: 2026-03-30*
*时间: 5:37 AM (Asia/Shanghai)*

---

## 2026-03-30 上午9:40 | 定时任务执行（第三次）

### 游戏开发：Minesweeper Raylib ✅

**项目**: `projects/minesweeper-raylib/`
**Commit**: `942cf17` → pushed to branch `minesweeper-raylib`
**编译**: ✅ 零警告通过
**特性**: 经典9x9扫雷 + Raylib图形渲染 + 计时器+最高分

**核心功能**:
| 功能 | 实现 |
|------|------|
| 9x9网格，10个雷 | 经典入门配置 |
| 左键翻开 | reveal + flood fill |
| 右键标记旗帜 | toggle flag |
| 首次点击安全区 | 3x3区域内不放雷 |
| BFS flood fill | 空白格自动扩展 |
| 计时器 | 每秒递增 |
| 最高分持久化 | minesweeper_best.txt |
| 3D立体格子 | 凸起/凹陷视觉效果 |
| 地雷爆炸动画 | 圆形+8条辐射线 |
| 旗帜三角形 | DrawTriangle绘制 |
| R键重新开始 | 全局重置 |
| 胜利/失败叠加层 | 半透明遮罩 |

**编译参数**:
```
gcc src/main.c src/game.c src/draw.c -o minesweeper_raylib \
    -Wall -Wextra -O2 \
    -I/opt/homebrew/include -L/opt/homebrew/lib -lraylib -lm
```

**Raylib绘图技巧**:
- `DrawRectangle` + 两条线模拟3D凸起/凹陷
- `DrawCircle` + `DrawLine` 画地雷
- `DrawTriangle` 画旗帜
- `MeasureText` + 居中算偏移

---

### LeetCode 练习（第三次）

| 题目 | 分类 | 难度 | 状态 |
|------|------|------|------|
| LC329 最长递增路径 | DFS+记忆化 | Hard | ✅ |

**LC329 关键技巧**: 
- `memo[i][j]` 记忆化搜索，从每个格子DFS
- 4方向扩展，只走严格递增路径
- 时间O(MN)，空间O(MN)
- 答案取所有格子的最大值

---

### 本周累计更新（2026-03-25 ~ 2026-03-30 上午）

| 指标 | 数量 |
|------|------|
| LeetCode 完成 | **57+ 道** |
| Hard 题目 | **28 道** |
| ncurses 游戏 | 8 个 |
| Raylib 图形游戏 | 5 个 |
| Web 游戏 | 1 个 |
| 本周游戏总计 | **15 个** 🎉 |

### 本次新增LeetCode

| 题目 | 算法 |
|------|------|
| LC329 最长递增路径 | DFS+记忆化 |

### 本次新增游戏

| 游戏 | 平台 | Branch |
|------|------|--------|
| Minesweeper | Raylib | minesweeper-raylib |

---

## GitHub Push

- ✅ `942cf17` — Minesweeper Raylib (pushed to `minesweeper-raylib` branch)

---

## 技术沉淀

### Raylib 绘图模式

1. **3D凸起格子**（隐藏状态）:
   ```c
   DrawRectangle(x, y, s, s, CLR_CELL_HIDDEN);
   DrawRectangle(x, y, s, 3, LIGHT_BEVEL);   // top
   DrawRectangle(x, y, 3, s, LIGHT_BEVEL);    // left
   DrawRectangle(x, y+s-3, s, 3, DARK_BEVEL); // bottom
   DrawRectangle(x+s-3, y, 3, s, DARK_BEVEL); // right
   ```

2. **3D凹陷格子**（翻开状态）:
   ```c
   DrawRectangle(x, y, s, s, CLR_CELL_REVEAL);
   DrawRectangle(x, y, s, 2, DARKER);  // top → darker (light from top-left)
   DrawRectangle(x, y, 2, s, DARKER);  // left
   ```

3. **地雷**:
   ```c
   DrawCircle(cx, cy, r, MINE_COL);           // body
   DrawCircle(cx, cy, r*0.6, BRIGHT_COL);     // highlight
   for (int a=0; a<8; a++) {                   // spokes
       int ex = cx + cos(a*PI/4)*r*1.3;
       int ey = cy + sin(a*PI/4)*r*1.3;
       DrawLine(cx, cy, ex, ey, MINE_COL);
   }
   ```

4. **旗帜**:
   ```c
   DrawLine(px, top, px, bot, GRAY_POLE);
   DrawTriangle((Vector2){px, top},
                (Vector2){px+10, top+6},
                (Vector2){px, top+12}, FLAG_COL);
   ```

### Raylib 头文件注意

- `game.h` 需要同时 include `<stdbool.h>` + `<raylib.h>`
- `draw.c` 需要额外 `#include <stdio.h>` (snprintf)
- `game.c` 不需要 raylib.h（纯逻辑）

---

*记录人: 悟通 (开发者 Agent)*
*日期: 2026-03-30*
*时间: 9:40 AM (Asia/Shanghai)*

---

## 2026-03-30 上午11:40 | 定时任务执行（第四次）

### LeetCode 练习

| 题目 | 分类 | 难度 | 状态 |
|------|------|------|------|
| LC403 青蛙过河 | DFS+记忆化+二分查找 | Hard | ✅ |
| LC410 分割数组最大和 | 二分+贪心验证 | Hard | ✅ |
| LC42 接雨水 | 双指针 | Hard | ✅ |
| LC76 最小覆盖子串 | 滑动窗口 | Hard | ✅ |

**LC403 关键技巧**: 
- 状态 `(i, k)` = 在第i块石头上，上次跳了k步
- 用 `unordered_map<int, unordered_set<int>>` 记忆化
- 二分查找目标石头的下标，O(N) 判断可达性

**LC410 关键技巧**: 
- 二分答案范围 `[max(nums), sum(nums)]`
- `canSplit()` 贪心：尽量装满当前子数组，计数超m则失败

**LC42 关键技巧**: 
- 左右双指针，谁小移谁
- `leftMax` / `rightMax` 记录当前最大边界
- 面积 += `max(0, min(leftMax, rightMax) - height[i])`

**LC76 关键技巧**: 
- `need[]` 和 `window[]` 计数数组
- `formed` 计数器：满足条件的字符种类数
- 右扩找解，左缩优化

---

### 游戏开发：Breakout Raylib ✅

**项目**: `projects/breakout-raylib/`
**Commit**: `de72034`
**新建 GitHub Repo**: `chongjie-ran/raylib-games`
**编译**: ✅ 零警告通过（仅C++模式警告）
**特性**: 经典打砖块 + 挡板角度反弹 + 关卡递进

**核心功能**:
| 功能 | 实现 |
|------|------|
| 5行×10列砖块 | 按行着色（红橙黄绿蓝） |
| 挡板角度反弹 | 根据击中位置计算反弹角度 |
| 3条命 | 球落底扣命，0命Game Over |
| 计分系统 | 按砖块所在行计分（顶行10分×行号） |
| 关卡递进 | 清除所有砖块进入下一关 |
| 暂停 | P键暂停/继续 |
| 首次点击安全 | 首次发球前球跟随挡板 |

**编译参数**:
```bash
g++ src/main.c src/game.c src/draw.c -o breakout_raylib \
    -Wall -Wextra -O2 \
    -I/opt/homebrew/include -L/opt/homebrew/lib -lraylib -lm
```

**工程结构**:
```
breakout-raylib/
├── src/
│   ├── main.c    # 入口 + 输入处理
│   ├── game.h    # 游戏状态结构体定义
│   ├── game.c    # 游戏逻辑（碰撞/砖块/物理）
│   ├── draw.h    # 渲染声明
│   └── draw.c    # Raylib渲染（砖块/挡板/球/UI）
├── breakout_raylib  # 编译产物
├── README.md
└── .gitignore
```

---

### 本周累计更新（2026-03-25 ~ 2026-03-30 上午11:40）

| 指标 | 数量 |
|------|------|
| LeetCode 完成 | **61+ 道** |
| Hard 题目 | **32 道** |
| ncurses 游戏 | 8 个 |
| Raylib 图形游戏 | 6 个 |
| Web 游戏 | 1 个 |
| 本周游戏总计 | **16 个** 🎉 |

### 本次新增LeetCode（+4 Hard）

| 题目 | 算法 |
|------|------|
| LC403 青蛙过河 | DFS记忆化+二分查找 |
| LC410 分割数组最大和 | 二分+贪心 |
| LC42 接雨水 | 双指针 |
| LC76 最小覆盖子串 | 滑动窗口 |

### 本次新增游戏

| 游戏 | 平台 | Commit |
|------|------|--------|
| Breakout | Raylib | de72034 🆕 |

---

## GitHub Push

- ✅ `de72034` — Breakout Raylib (new repo: `chongjie-ran/raylib-games`)

---

## 技术沉淀

### Breakout 挡板反弹物理

```c
// 根据击中挡板的位置计算反弹角度
float hit_pos = (b->x - paddle.x) / PADDLE_W; // 0..1
float angle = 150.0f + hit_pos * 120.0f; // 150°~270°
float rad = angle * M_PI / 180.0f;
b->dx = speed * cosf(rad);
b->dy = speed * sinf(rad);
```

### Raylib C/C++ 编译注意

- `.c` 文件被 `clang++` 当 C++ 编译会有 deprecated 警告，无害
- `game.c`（纯逻辑）不需要 `#include <raylib.h>`，分离关注点
- `draw_paused(Game* g)` 中用 `(void)g;` 消除 unused parameter 警告

### 滑动窗口模板（LC76）

```c
int formed = 0, required = 0;
for (int i = 0; i < 256; i++) if (need[i] > 0) required++;

while (right < n) {
    // 扩展右指针
    window[c]++; if (need[c] > 0 && window[c] == need[c]) formed++;

    // 收缩左指针找最优
    while (formed == required) {
        update_best();
        char cl = s[left];
        if (need[cl] > 0 && window[cl] == need[cl]) formed--;
        window[cl]--; left++;
    }
    right++;
}
```

---

*记录人: 悟通 (开发者 Agent)*
*日期: 2026-03-30*
*时间: 11:40 AM (Asia/Shanghai)*
